# Планер (исходники)

В корне проекта лежит только **`Planer.exe`** — один файл для запуска.

## Запуск

Запустите `..\Planer.exe` из корня проекта.

## Сборка exe

```bat
build.bat
```

Создаёт/обновляет `..\Planer.exe` в корне проекта.

## Разработка в браузере

Откройте `index.html` в браузере.
