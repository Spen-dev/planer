# Copy to keystore.properties and adjust paths/passwords.
# Generate keystore: .\create-keystore.ps1  or  ./create-keystore.sh
storeFile=keystore/planer-release.jks
storePassword=change-me
keyAlias=planer
keyPassword=change-me
